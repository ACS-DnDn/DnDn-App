[project]
name = "dndn-report"
version = "0.1.0"
description = "DnDn Report Builder - AI 보고서 생성"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "python-dotenv>=1.0.0",
]
